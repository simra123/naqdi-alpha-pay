import { useEffect } from "react";

const useStickyColumns = (stickyColumns: string[]) => {
  useEffect(() => {
    // Add any logic for dynamically setting sticky styles
  }, [stickyColumns]);
};

export default useStickyColumns;
